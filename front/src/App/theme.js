import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#2d301e',
    },
    secondary: {
      main: '#9fac5c',
    },
    default:{
      main: '#b88262'
    }
  },
});

export default theme;